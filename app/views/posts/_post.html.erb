<div id="<%= dom_id post %>">
  <p class="text-sm font-bold text-gray-500">Title</p>
  <p class="text-lg text-gray-900 mb-4"><%= post.title %></p>

  <p class="text-sm font-bold text-gray-500">Content</p>
  <div class="prose max-w-none">
    <%= post.content %>
  </div>
</div>